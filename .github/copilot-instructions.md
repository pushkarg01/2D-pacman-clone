# Copilot Instructions

## Project Guidelines
- User wants Dependency Injection implemented in the Unity project using Zenject.
- Use a centralized `GameEvents` event aggregator pattern across the Unity project, with other scripts subscribing to it.